import React from 'react';
import './WelcomeScreen.css';

const WelcomeScreen = () => {
    return (
        <div className="welcome-screen">
            <h1>Bem-vindo ao nosso serviço de LLMs!</h1>
            <p>Selecione um modelo para começar a conversar.</p>
        </div>
    );
};

export default WelcomeScreen;